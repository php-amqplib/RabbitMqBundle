language: php

php:
  - 5.3
  - 5.4

env:
  - SYMFONY_VERSION=v2.0.*
  - SYMFONY_VERSION=v2.1.*

before_script:
    - curl -s http://getcomposer.org/installer | php
    - php composer.phar install --dev --prefer-source
