old_sound_rabbit_mq:

    connections:
        default:

    producers:
        producer:
            connection: default
            validator_class:  null
            definitions:      null
            schema:           null